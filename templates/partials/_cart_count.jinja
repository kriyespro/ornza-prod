<span class="cart-count absolute -top-1 -right-1 w-4 h-4 rounded-full bg-gold text-espresso text-[9px] font-bold flex items-center justify-center{% if count == 0 %} invisible{% endif %}" id="cart-count">{{ count }}</span>
